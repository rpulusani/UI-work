define(['angular', 'filterSearch'], function(angular) {
    'use strict';
    angular.module('mps.filterSearch')
    .controller('AccountFilterController', ['$scope', '$translate', 'OrderStatus',
        function($scope, $translate, OrderStatus) {
            
        }
    ]);
});
