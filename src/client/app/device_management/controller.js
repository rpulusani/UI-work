'use strict';
angular.module('mps.deviceManagement')
.controller('DeviceManagementController', ['$scope', '$location', '$routeParams', 'History',
    function($scope, $location, $routeParams, History) {

    }
]);
